language: r
distro: trusty
sudo: required
warnings_are_errors: false

addon:
  packages:
   - python-gdal
   - libgdal1-dev

# Code coverage
r_packages:
  - covr
  - rgdal

before_script:
  - sudo apt-get install -y gdal-bin

after_success:
- Rscript -e 'library(covr); codecov()'
